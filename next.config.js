/** @type {import('next').NextConfig} */
const nextConfig = {
  // Không sử dụng static export vì có API routes
  // output: 'export',
  // trailingSlash: true,
  // images: {
  //   unoptimized: true
  // }
};

module.exports = nextConfig;
